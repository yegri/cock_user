import { globalStyle, style } from "@vanilla-extract/css";

export const root = style({});

export const searchInput = style({
  height: "2rem",
});

export const searchBtn = style({
  padding: "0.3rem 1rem",
  height: "2rem",
  background: "rgba(40,22,91,0.7)",
  color: "#fff",
});
